# Gates

Results of each check and campaign case, pass or fail, in the order they ran. Failed runs stay here with their evidence.

| Time (WAT) | Gate | Result | Evidence |
| --- | --- | --- | --- |
| 2026-09-25 11:20 | Day-one: fork clock, Jupiter route, fork signatures, CPI with PDA taker | 3 of 4 PASS; the Jupiter route and the CPI check failed on a proprietary AMM hop | `evidence/day-one/2026-09-25T10-20-11-643Z.json` |
| 2026-09-25 11:22 | Day-one re-run with proprietary AMMs excluded on the fork | 4 of 4 PASS | `evidence/day-one/2026-09-25T10-21-50-930Z.json` |
| 2026-09-25 14:29 | `pnpm demo:fork` rehearsal from main on the verifiable build of main@eade508 (solana-verify `be0602b7…876b`) | Detected 4 s after the transfer, recorded 1 s later (370 of 1,850 USDC invested). SPYx and NVDAx wait PRICE_UNAVAILABLE (Hermes 403, key lacks the equities grant). OpenAI waits PREMIUM_TOO_HIGH from the program's simulation. Anthropic FAILED: first attempt rejected in Flux (fork-stale DEX, excluded and re-quoted), second hit InputOverspent from leftover wSOL on a two-hop route (fixed by #32) | `evidence/2026-09-25T13-29-17-861Z/` |
| 2026-09-25 15:25 | Canonical fork run, attempt 1, on the verifiable build of main@e53fbfe (solana-verify `8412a604…8cc4`) | FAILED: `solana program deploy` stalled for 11 minutes with the surfnet idle after its account fetches from the public mainnet datasource; stopped and retried | `evidence/2026-09-25T14-25-46-561Z/surfpool.log` |
| 2026-09-25 15:38 | Canonical fork run, attempt 2 (main@44315ca, same binary) | Paycheck detected 1.5 s after the transfer and recorded (370 of 1,850 USDC). Anthropic VERIFIED: 86,315,830 raw delivered + 871,878 issuer fee withheld against a 84,991,267 minimum, 40 bps over the attested mark, 162 bps all-in, every verifier check passed. OpenAI waits PREMIUM_TOO_HIGH (program simulation OutputBelowMinimum; quote 30.4% over mark). SPYx and NVDAx wait PRICE_UNAVAILABLE (Pyth key lacks the equities grant) | `evidence/stocklana-fork/` |
| 2026-09-25 14:31–18:05 | Evaluation campaign P1–P8, 32 runs, each on a fresh fork during the US regular session (summary: `evidence/campaign/summary.json`, re-derived byte for byte by `pnpm verify:campaign`) | pass 4, fail 14, blocked 2, infrastructure 12. P2 controls PASS. P5 stale attestation and multiplier change PASS on the final run. P6 replay PASS on the fixed binary. P7 ablations PASS. P1 final run BLOCKED: Anthropic verified at 42 bps over mark, NVDAx waited on the Pyth entitlement. P8 final run 27 of 42 probes: every fill verified (Anthropic, Figure AI and Kalshi at $20 and $2,500; QQQx and TSLAx at $250 and $5,000), OpenAI, Anduril, Polymarket and Neuralink waited PREMIUM_TOO_HIGH, seven xStocks waited on the entitlement, and one Neuralink leg stopped because the PreStocks API returned a non-number mark for one entry. Early P1/P8 runs also hit the leftover-wSOL bug before PR #32. P4 harmful actions, final run 4 of 5 probes matched: shares routed to another account were refused with DestinationOwnerMismatch, and shares diverted with the owner named were refused with OutputBelowMinimum. The attack that swaps the route's source for the owner's pay-in was refused by Jupiter's own program (error 6025) before the program's InputOverspent check ran, so it's recorded as not the expected error. Nothing moved, and the honest crank then executed the slice to Verified. Four earlier P4 runs failed on stale prices, rate limits or RPC errors and are kept. P4 real market: OpenAI waited PREMIUM_TOO_HIGH through six rounds and was expired by `expire_leg`; Neuralink's fifth retry hit a network error and stayed pending. P3 BLOCKED by the entitlement. Every run is labelled with its binary | `evidence/campaign/` |
| 2026-09-25 18:05 | Decision rules (fork) | Integrity PASS (20/20 executed slices Verified). Cost INCONCLUSIVE (median $1.00 per $100, n=15, on the fail line). Guard FAIL (no slice waited and later filled), so the guard is described only as protection. Speed not evaluated on a fork | `evidence/campaign/summary.json` decisionRules |
| 2026-09-25 19:50 | Freeze: main@18a0a60 CI green; `check:facts` and `verify:bundle` pass; `stocklana-submission` tagged; release build reproduces the canonical binary | PASS: release `paycheck_router.so` executable hash `8412a604…8cc4` equals the binary the canonical fork run deployed | [release](https://github.com/winsznx/paycheck-router/releases/tag/stocklana-submission), run 36175589252 |
