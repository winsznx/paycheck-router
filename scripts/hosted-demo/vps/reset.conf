[Service]
# The demo fork resets every 6 hours from its snapshot: systemd stops it after 6 h of runtime
# and Restart=always brings up a fresh fork, so the reset time always equals start + 6 h.
RuntimeMaxSec=6h
